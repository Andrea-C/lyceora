/** The subject catalog — grows as multi-subject paths are added. */
export const SUBJECTS = [{ subjectId: "math", pathId: "path_recupero_media", available: true }] as const;
export const RECOVERY_PATH_ID = "path_recupero_media";
