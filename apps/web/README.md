# @lyceora/web

The Next.js app for Lyceora. See the [root README](../../README.md) for setup, environment
variables, testing, and deployment — this package has no separate instructions.

Quick links: `pnpm dev` (from repo root) starts this app; `pnpm --filter web test:e2e` runs its
Playwright suite.
